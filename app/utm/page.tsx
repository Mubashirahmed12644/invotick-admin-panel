import UtmPageClient from "./UtmPageClient";

export const metadata = {
  title: "UTM & Attribution | invotics",
  description: "Build tagged campaign links and track install attribution",
};

export default function UtmPage() {
  return <UtmPageClient />;
}
